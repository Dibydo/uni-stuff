(define-syntax trace-ex
  (syntax-rules ()
    ((_ expr)
     (begin
       (display 'expr)
       (display " => ")
       (let ((result expr))
       (write result)
       (newline)
         result)))))

;(load "1.scm.txt")
;(define (zip . xss)
;  (if (or (null? xss)
;          (null? (trace-ex (car xss))))
;      '()
;      (cons (map car xss)
;            (apply zip (map cdr (trace-ex xss))))))